    #Python 5
import numpy as np
import math

def main():
    x_values = np.linspace(0, 2 * math.pi, 1000)
    print("x\t\t sin(x)")
    print("----------------------")
    for x in x_values:
        print(f"{x:.6f}\t {math.sin(x):.6f}")
main()
